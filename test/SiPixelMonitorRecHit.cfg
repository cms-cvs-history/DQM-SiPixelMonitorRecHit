process  processPixelMonitorRecHit =
{

   # The file listed here is the generic output of the
   # Sim-Reco-Digi tutorial package.  The actual file was not included
   # due to space issues.  Replace this file name with your own input.

   untracked PSet maxEvents = {untracked int32 input = -1}
   source = PoolSource {
                untracked vstring fileNames = {'file:/afs/cern.ch/user/k/krose/scratch0/CMSSW_1_6_0_pre4/src/SimG4Core/Application/test/evtgen_detsim_digi.root'}
              
                untracked uint32 debugVebosity = 10
                untracked bool   debugFlag     = true
   }


  # Initialize magnetic field
  include "MagneticField/Engine/data/volumeBasedMagneticField.cfi"

  # Tracker SimGeometryXML
  include "Geometry/TrackerSimData/data/trackerSimGeometryXML.cfi"

  # Tracker Geometry Builder
  include "Geometry/TrackerGeometryBuilder/data/trackerGeometry.cfi"

  # Tracker Numbering Builder
  include "Geometry/TrackerNumberingBuilder/data/trackerNumberingGeometry.cfi"

  # Pixel RecHit Validation
  include "DQM/SiPixelMonitorRecHit/data/SiPixelMonitorRecHit.cfi"

  path p1 = { PixelMonitorRecHit }

  service = MessageLogger {
          untracked vstring destinations = {   "detailedInfo.txt"
                               , "critical.txt"
                               , "cerr"
                            }
          untracked PSet critical.txt     = { untracked string threshold = "ERROR"   }
          untracked PSet detailedInfo.txt = { untracked string threshold = "INFO"    }
          untracked PSet cerr             = { untracked string threshold = "WARNING" }
        }

  service = DaqMonitorROOTBackEnd{ }
}
